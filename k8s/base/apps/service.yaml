# Service отвечает за стабильное DNS-имя и маршрутизацию трафика на поды Deployment.
# Через него другие сервисы обращаются к book-service внутри кластера.

apiVersion: v1
kind: Service
metadata:
  name: reader-service      
                           
  namespace: default
  labels:
    app: reader-service
    env: production

spec:
  type: ClusterIP

  selector:
    app: reader-service      


  ports:
    - name: http
      protocol: TCP
      port: 8000           
      targetPort: http     