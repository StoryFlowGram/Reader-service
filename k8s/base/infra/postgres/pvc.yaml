apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-reader-data
  namespace: default
  labels:
    app: postgres-reader
    env: production
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi


      